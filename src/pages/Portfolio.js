import React from 'react';

const Portfolio = () => {
    return (
        <section className="content" id="portfolio">
            <h1>Portfolio</h1>
        </section>
    );
};

export default Portfolio;